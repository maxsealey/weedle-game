const initialState = {
    isLoading: false,
    isError: false,
    error:'error'
}

export const pokemonReducer = (state = initialState, action) => {
    switch(action.type){
        default:
            return state
    }
}